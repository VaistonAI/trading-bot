rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is authenticated
    function isSignedIn() {
      return request.auth != null;
    }
    
    // Helper function to get user data
    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }
    
    // Helper function to check if user is admin
    function isAdmin() {
      return isSignedIn() && getUserData().role == 'admin';
    }
    
    // Helper function to check if user is admin or investor
    function canViewReports() {
      return isSignedIn() && (getUserData().role == 'admin' || getUserData().role == 'investor');
    }
    
    // Users collection
    match /users/{userId} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
      
      // User's trading data
      match /trading/{document=**} {
        allow read: if isSignedIn();
        allow write: if isAdmin();
      }
    }
    
    // Daily Reports collection
    match /dailyReports/{reportId} {
      // Only admins and investors can read reports
      allow read: if canViewReports();
      // Only backend can write (no client writes allowed)
      allow write: if false;
    }
    
    // Simulations collection
    match /simulations/{year} {
      // Only admins and investors can read simulations
      allow read: if canViewReports();
      // Admins and investors can write simulations from frontend
      allow write: if canViewReports();
    }
    
    // Simulations Momentum collection
    match /simulations-momentum/{year} {
      // Only admins and investors can read simulations
      allow read: if canViewReports();
      // Admins and investors can write simulations from frontend
      allow write: if canViewReports();
    }
    
    // Simulations Growth collection
    match /simulations-growth/{year} {
      // Only admins and investors can read simulations
      allow read: if canViewReports();
      // Admins and investors can write simulations from frontend
      allow write: if canViewReports();
    }
    
    // Simulations Mean Reversion collection
    match /simulations-meanreversion/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Simulations Breakout collection
    match /simulations-breakout/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Simulations RSI collection
    match /simulations-rsi/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Simulations MACD collection
    match /simulations-macd/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Simulations Volume Weighted collection
    match /simulations-volumeweighted/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Simulations Pairs Trading collection
    match /simulations-pairs/{year} {
      allow read: if canViewReports();
      allow write: if canViewReports();
    }
    
    // Invitations collection
    match /invitations/{invitationId} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }
    
    // Default: deny all access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
